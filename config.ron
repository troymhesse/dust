SimulationConfig(
    driver: DriverConfig(
        checkpoint_interval: None,
        output_dir: ".",
    ),
    physics: DustPhysics(
        tfinal: 1000000.0,
        dt: 0.001,
        softening: 0.05,
        central_mass: 1.0,
    ),
    initial: DustInitial(
        num_particles: 200,
        setup: Ring,
    ),
    compute: DustCompute(),
)